'use strict';

/** @type {import('sequelize-cli').Migration} */
module.exports = {
  async up(queryInterface, Sequelize) {
    await queryInterface.createTable("stud_metals", {
      id: {
        type: Sequelize.BIGINT,
        allowNull: false,
        primaryKey: true,
        autoIncrement: true,
        autoIncrementIdentity: true,
      },
      stud_id: {
        allowNull: false,
        type: Sequelize.BIGINT,
        references: {
          model: "stud_config_products",
          key: "id",
          as: "fk_stud_id",
        }
      },
      metal_id: {
        allowNull: false,
        type: Sequelize.BIGINT,
        references: {
          model: "metal_masters",
          key: "id",
          as: "fk_metal_id",
        }
      },
      karat_id: {
        type: Sequelize.BIGINT,
        references: {
          model: "gold_kts",
          key: "id",
          as: "fk_karat_id",
        }
      },
      metal_wt: {
        allowNull: false,
        type: Sequelize.DOUBLE,
      },
      company_info_id: {
        type: Sequelize.INTEGER
      },
    })

    await queryInterface.sequelize.query(`
      ALTER TABLE IF EXISTS public.company_infoes
    ALTER COLUMN id SET GENERATED BY DEFAULT;`);
  },

  async down(queryInterface) {
    await queryInterface.dropTable('stud_metals');
  }
};
